# Replace This Folder With the Shared Zip

Extract `transitrisk_data_models.zip` into the repo root.
It will replace this entire `models/` folder with all 7 trained models.

See README.md for full instructions.
